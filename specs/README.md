# specs

Index for specification docs (design/contract).

- `spec.md` - full architecture spec (dendritic pattern, module contract, environments, workflows).

When changing behavior, update `spec.md` first, then align code + root `README.md`.
