# editors
Purpose: editor toolchains + editor config modules.
Expand: add `<editor>.nix`; include binaries/plugins/config; expose `devShells.<editor>`; include in environment composition when needed.
