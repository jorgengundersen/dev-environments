# utils
Purpose: generic cross-domain utility tool shells.
Expand: add `<tool>.nix` only for tools that do not fit existing domains (`ai`, `docs`, `languages`, `quality`, `data`, `git`, `shell`, `editors`); expose `devShells.<tool>` and include in environment composition when needed.
