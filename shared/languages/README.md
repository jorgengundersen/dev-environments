# languages
Purpose: language toolchain shells.
Expand: add `<lang>.nix`; declare compiler/lsp/lint in `devShells.<lang>`; optional env vars/home module; include in environment composition when needed.
